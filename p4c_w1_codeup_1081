#include <stdio.h>
int main(){
	int i, j, n, m; //i는 i<=n인 임의의 변수, j는 j<=m인 임의의 변수로 선 
	scanf("%d %d", &n, &m)언;

	for(i=1; i<=n; i++)
	{
	  for(j=1; j<=m; j++)
	  {
	    printf("%d %d \n", i, j);
	  }
	}
		return 0;
	}
	
	/*가장 어려웠던 문제는 아니었다. 가장 어려웠던 문제들은
	93번부터 이어지는 문제들이었고 이조차도 각각 1093번,  1094번, 1097번이 
	해결되면 비슷한 패턴으로 풀 수 있었다. 
	하지만 사소한 오류들이 시간을 많이 끌었는데, 
	#1073에서의 오류뿐만 아니라
	이 문제에서도 printf가 아닌 prinf 로 써놓고 한참 찾지 못해서 
	컴퓨터의 문제인줄알고 재부팅을 몇번씩이나 했었다.
	1097번으로 write up을 할까  1081번으로 할까 고민을 하다, 
	prinf를 발견한 순간의 고통이 떠올라 1081번으로 하기로 결정했다.*/ 
