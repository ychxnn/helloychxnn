/*
#include <stdio.h>
int main(){
	char a;
	while(a!='q'){
		scanf("%d", &a);
		printf("&d", a);
	}
	return 0;
}

#1079 선정 이유 : syntax를 아무리 잘 이해하고 있어도 꼼꼼히 체크하지 못하면 결과값을 얻을 수 없다.
1. line 6,7 : a는 문자인데 서식지정자를 %d로 놓아 error
2. line7 : 코딩도장, codeup 을 모두 끝내면서 가장 많이 범했던 오류 : 몰라서 % 대신 &를 붙인 것은 아니고
          타자를 빨리 치려다 보니 % 대신 &를 쳐서 정확한 값이 출력되지 않는 경우가 많았다.
*/

#include <stdio.h>
int main(){
	char a;
	while(a!='q'){
		scanf("%d", &a);
		printf("%d", a);
	}
	return 0;
}
//#1079
//#P4C
